{% extends 'index.html' %}

{% block content %}
  <h1>Deploy</h1>
    <table class="table table-bordered">
      <thead>
        <th>UID</th>
        <th>Nombre</th>
        <th>Fecha de expiración</th>
        <th>Propietario</th>
        <th>Plan</th>
        <th colspan="2" class="text-center">Acciones</th>
      </thead>
      <tbody>
        {% for deploy in data %}
        <tr>
          <td>{{ deploy.id }}</td>
          <td>{{ deploy.vmname }}</td>
          <td>{{ deploy.expiry_time }}</td>
          <td>{{ deploy.user_name }} {{ deploy.last_name}}</td>
          <td>{{ deploy.name }}</td>
          <td>
            <a href="">Editar</a>
          </td>
          <td>
            <a href="">Eliminar</a>
          </td>
        </tr>
        {% endfor %}
      </tbody>
    </table>

  <div class="floating">
    <a href="{{ URL }}/deploy/create" class="btn btn-primary btn-fab">
      <i class="material-icons">add</i>
    </a>
  </div>
{% endblock %}